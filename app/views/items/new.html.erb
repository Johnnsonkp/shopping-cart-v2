<section id="new-item">
    <%= render 'showcase' %>
    <div class="container">
        <h1>Add New Item</h1>

        <%= render 'form', item: @item %>

        <%= link_to 'Back', items_path, class:"back-btn" %>
    </div>
</section>

